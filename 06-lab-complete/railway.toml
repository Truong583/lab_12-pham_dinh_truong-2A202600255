[build]
builder = "DOCKERFILE"

[deploy]
# Sử dụng python -m để uvicorn.run trong main.py tự xử lý PORT và HOST
startCommand = "python -m app.main"
healthcheckPath = "/health"
healthcheckTimeout = 45
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
